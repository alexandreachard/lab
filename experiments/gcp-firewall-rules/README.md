# Firewall Rules Test

In this experiment, I provision precise VPC firewall rules to control ingress traffic paths, specifically targeting Google Cloud health check/load balancer ranges and Identity-Aware Proxy (IAP) secure tunnel allocations.

## Usage

I use this snippet to test my network boundary perimeters, mapping out external load balancer proxies and administrative jumpbox access configurations:

> I execute this directly within the network spoke project context.