# Compute Instance Test

In this experiment, I provision a highly locked-down Google Compute Engine (GCE) VM instance attached to a dedicated custom service account. The configuration utilizes customer-managed encryption keys (CMEK), static internal IP allocation, and OS Login authorization.

## Usage

I use this snippet to test a secure, non-public development instance blueprint within a controlled VPC subnetwork: