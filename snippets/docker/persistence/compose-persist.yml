services:
  nginx:
    networks:
      - frontend
      - backend
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - nginx-logs:/var/log/nginx

  db:
    networks:
      - backend

  volumes:
    nginx-logs:
